{% set name = "diaspora-stream-api" %}
{% set version = "0.5.3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/diaspora-project/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: fb68bbf7bf38b19b0c7a8465eca5802b3ce5f9ed4cb9561601e08694a648b842

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake >=3.21
    - make
    - pkg-config
  host:
    - nlohmann_json
    - nlohmann-json-schema-validator
    - spdlog
    - yaml-cpp
    - tclap
    # Python support
    - pybind11
    - python
  run:
    - nlohmann_json
    - nlohmann-json-schema-validator
    - spdlog
    - yaml-cpp
    - python

test:
  commands:
    - test -f $PREFIX/include/diaspora/DataDescriptor.hpp
  imports:
    - diaspora_stream.api

about:
  home: https://github.com/diaspora-project/diaspora-stream-api
  license: MIT
  license_family: MIT
  summary: An API for distributed event-streaming service for HPC
  description: |
    An API for distributed event-streaming service for HPC.

extra:
  recipe-maintainers:
    - mdorier
