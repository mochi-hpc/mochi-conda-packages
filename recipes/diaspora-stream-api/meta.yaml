{% set name = "diaspora-stream-api" %}
{% set version = "0.5.6" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/diaspora-project/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: cd72d835991fa98ed498c8415018e15dd862ad12c8c4aacb7cd71cd269cd7ecd

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake >=3.21
    - make
    - pkg-config
  host:
    - nlohmann_json
    - nlohmann-json-schema-validator
    - spdlog
    - yaml-cpp
    - tclap
    # Python support
    - pybind11
    - python
  run:
    - nlohmann_json
    - nlohmann-json-schema-validator
    - spdlog
    - yaml-cpp
    - python

test:
  commands:
    - test -f $PREFIX/include/diaspora/DataDescriptor.hpp
  imports:
    - diaspora_stream.api

about:
  home: https://github.com/diaspora-project/diaspora-stream-api
  license: MIT
  license_family: MIT
  summary: An API for distributed event-streaming service for HPC
  description: |
    An API for distributed event-streaming service for HPC.

extra:
  recipe-maintainers:
    - mdorier
